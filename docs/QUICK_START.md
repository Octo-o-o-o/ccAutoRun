# Quick Start - 5分钟快速上手 🚀

> 从安装到第一次成功执行任务的完整指南

## 📋 前置条件

确保你的系统已安装：
- **Node.js** >= 18.0.0 ([下载](https://nodejs.org/))
- **Claude Code** 最新版本 ([安装指南](https://docs.claude.com/claude-code))
- **Git** >= 2.20.0

## ⚡ 快速安装

### 方式1：npm 全局安装（推荐）
```bash
npm install -g ccautorun
```

### 方式2：npx 直接使用（无需安装）
```bash
npx ccautorun --version
```

### 方式3：从源码安装（开发者）
```bash
git clone https://github.com/yourusername/ccAutoRun.git
cd ccAutoRun
npm install
npm link
```

## 🎯 首次使用（3步走）

### Step 1: 初始化项目
在你的项目目录运行：
```bash
cd /path/to/your/project
ccautorun init
```

你会看到交互式向导：
```
👋 欢迎使用 ccAutoRun v2.0！

✓ 检测到 Claude Code 已安装
✓ 检测到 Git 仓库

? 是否自动配置 Claude Code hooks？(Y/n) Y
✓ Hooks 配置完成！

📁 已创建工作目录: .ccautorun/
   ├── plans/        (执行计划存储)
   ├── sessions/     (会话记录)
   └── config.json   (配置文件)

🎉 初始化完成！运行 'ccautorun --help' 查看所有命令
```

### Step 2: 生成第一个任务计划
在 Claude Code 中使用 `/plan` 命令：
```
/plan 我需要为项目添加单元测试框架（使用 vitest）
```

Claude 会：
1. 分析你的项目结构
2. 生成详细的执行计划（EXECUTION_PLAN.md + stages/）
3. 询问你是否确认

确认后，计划保存到 `.ccautorun/plans/task-YYYYMMDD-HHMMSS/`

### Step 3: 自动执行任务
**方式A：自动继续模式（推荐）**
```bash
# 在执行计划目录中
cd .ccautorun/plans/task-20250107-120000

# 在 Claude Code 中输入
执行 Stage 1
```

Claude 会自动：
- 读取 Stage 1 的详细任务
- 执行所有步骤
- 完成后自动提示下一个 Stage

**方式B：手动控制模式**
```bash
# 查看计划状态
ccautorun status

# 列出所有计划
ccautorun list

# 手动执行特定 Stage
在 Claude Code 中：执行 docs/stages/stage-2.md
```

## 🎬 完整示例：添加功能

假设你想为项目添加深色模式：

```bash
# 1. 创建计划
# 在 Claude Code 中输入：
/plan 为应用添加深色模式切换功能（包含主题管理、持久化存储和UI切换）

# 2. Claude 生成计划后确认
# 你会看到：
✓ 已生成计划: .ccautorun/plans/dark-mode-20250107-143022/
  ├── EXECUTION_PLAN.md    (概览)
  └── stages/
      ├── stage-1.md       (创建主题系统)
      ├── stage-2.md       (实现切换UI)
      ├── stage-3.md       (持久化存储)
      └── stage-4.md       (测试和优化)

# 3. 开始执行
# 在 Claude Code 中：
执行 Stage 1

# 4. 自动继续
# Claude 完成 Stage 1 后会提示：
✅ Stage 1 已完成！
📋 下一步: Stage 2 - 实现切换UI
🚀 输入 "继续" 开始 Stage 2

# 5. 查看进度
ccautorun status

# 输出：
📊 当前计划: dark-mode-20250107-143022
   状态: 进行中
   进度: 2/4 (50%)
   ✅ Stage 1: 创建主题系统
   ✅ Stage 2: 实现切换UI
   ⏳ Stage 3: 持久化存储 (当前)
   ⏳ Stage 4: 测试和优化

# 6. 完成后归档
ccautorun archive dark-mode-20250107-143022
```

## 🛠️ 常用命令速查

| 命令 | 用途 | 示例 |
|------|------|------|
| `ccautorun init` | 初始化项目 | `ccautorun init` |
| `ccautorun list` | 列出所有计划 | `ccautorun list --status active` |
| `ccautorun status` | 查看当前计划状态 | `ccautorun status` |
| `ccautorun archive` | 归档已完成计划 | `ccautorun archive <plan-id>` |
| `ccautorun doctor` | 环境检查 | `ccautorun doctor` |
| `ccautorun config` | 配置管理 | `ccautorun config set auto_continue true` |
| `ccautorun recover` | 恢复失败任务 | `ccautorun recover --strategy retry` |
| `/plan` (Claude) | 生成执行计划 | `/plan 添加用户认证` |

## ⚙️ 推荐配置

编辑 `.ccautorun/config.json`：
```json
{
  "auto_continue": true,          // 自动继续下一 Stage
  "safety_limit": 10,             // 最大连续执行次数（0=无限制）
  "notifications": true,          // 启用系统通知
  "skip_permissions": false,      // 生产环境建议 false
  "default_architecture": "auto", // 自动选择架构（split/single/auto）
  "snapshot_retention": 5         // 保留最近 5 个快照
}
```

## 🎓 下一步学习

- 📖 [完整文档](../README.md) - 深入了解所有功能
- 📋 [任务模板库](TEMPLATES.md) - 常见任务的预设计划
- ❓ [常见问题](FAQ.md) - 解答疑惑
- 🔧 [故障排查](TROUBLESHOOTING.md) - 解决问题

## 💡 最佳实践提示

1. **计划描述要具体**
   - ❌ "优化代码"
   - ✅ "重构用户认证模块，使用JWT替代Session，添加单元测试"

2. **适时使用手动模式**
   - 复杂任务建议手动控制每个 Stage
   - 简单任务可以开启 `auto_continue`

3. **定期归档**
   ```bash
   ccautorun archive --older-than 30d
   ```

4. **出错时不要慌**
   ```bash
   ccautorun recover  # 选择恢复策略
   ```

5. **检查环境**
   ```bash
   ccautorun doctor
   # 确保所有依赖都正常
   ```

## 🎉 成功！

恭喜！你已经掌握了 ccAutoRun 的基础使用。现在可以：
- ✅ 用 AI 生成任务计划
- ✅ 自动化执行复杂任务
- ✅ 管理多个并行计划
- ✅ 处理错误和恢复

**有问题？** 查看 [FAQ](FAQ.md) 或 [提交 Issue](https://github.com/yourusername/ccAutoRun/issues)

---

**[← 返回主页](../README.md)** | **[查看模板 →](TEMPLATES.md)**

# Stage 2a: 核心CLI框架和基础命令 🔄

**[← 返回主计划](../../EXECUTION_PLAN.md)** | **[← Stage 1](stage-1.md)** | **[Stage 2b →](stage-2b.md)**

---

## 📋 阶段信息

- **阶段编号**: Stage 2a
- **预计时间**: 6-8小时
- **依赖**: Stage 1
- **状态**: ⏳ 待开始

### 🎯 目标
实现CLI命令框架、基础命令（init, list, status, doctor）和辅助工具

### 📝 任务清单
- [ ] 实现 `src/cli.js` 使用Commander.js
  - 定义所有命令结构
  - 版本号、帮助信息
  - 全局选项（--verbose, --dry-run, --config等）
  - **首次运行检测**：如果未初始化（没有 .ccautorun/），显示交互式 Onboarding
- [ ] 实现交互式 Onboarding（首次运行时）
  - 欢迎界面：`Welcome to ccAutoRun! 👋`
  - 提供选项：
    1. Initialize project (ccautorun init)
    2. Generate your first plan (claude "/plan <description>")
    3. Read documentation (opens docs link)
  - 根据用户选择执行对应操作
- [ ] 实现 `ccautorun init` 命令（增强版）
  - 支持 `--dry-run` 预览将要创建的文件
  - 在当前项目创建 `.ccautorun/` 目录
  - 创建 `plans/`, `sessions/`, `logs/`, `archive/` 子目录
  - 创建默认 `config.yaml`：
    ```yaml
    version: "2.0.0"
    default_architecture: auto  # auto=自动判断, split=强制拆分, single=强制单文件
    safety_limit: unlimited
    notifications:
      enabled: true
      sound: true
      progress_frequency: every-stage  # every-stage / every-2-stages / off
    auto_continue:
      enabled: true
      skip_permissions: true  # 自动继续时跳过权限确认（关键！）
    editor: "code"  # 打开计划文档的编辑器
    language: en    # CLI 输出语言（仅 en）
    ```
  - **自动配置 Claude Code hooks**（关键！）：
    - 支持 `--setup-hooks` 自动创建并配置 `.claude/hooks.yaml`
    - 支持 `--enable-hooks` 自动启用 auto-continue hook
    - 如果 `.claude/hooks.yaml` 不存在，创建并配置
    - 如果存在但 hook 未启用，交互式询问用户是否启用
    - 提示：制定计划不需要 hook，执行时才需要
- [ ] 实现 `ccautorun list` 命令
  - 扫描 `.ccautorun/plans/` 目录
  - **支持两种格式**：单文件（xxx.md）和拆分架构（xxx/README.md）
  - 解析每个计划的配置头
  - 显示任务列表（进度、状态、最后更新时间、架构类型）
- [ ] 实现 `ccautorun status [task-name]` 命令
  - 显示指定任务的详细状态
  - 当前阶段、会话计数、完成百分比
  - **拆分架构显示**：列出所有stages/文件
- [ ] 实现 `ccautorun doctor` 命令（诊断工具，关键！）
  - 支持 `--dry-run` 预览修复操作
  - 支持 `--fix` 自动修复常见问题
  - 检查 Claude CLI 是否安装（跨平台检测）
  - **检查 Claude CLI 版本**（关键！）：
    - 执行 `claude --version` 获取版本号
    - 检查是否满足最低版本要求（如 v1.0.0+）
    - 版本过旧时给出升级建议
  - 检查 `.claude/hooks.yaml` 是否正确配置
  - 检查 auto-continue hook 是否启用
  - 检查 Node.js 版本（>= 18）
  - 检查 `.ccautorun/` 目录权限
  - 验证当前任务状态（如果指定任务）
  - 验证计划文档格式（如果指定任务）
  - 输出诊断报告和修复建议
  - **自动修复**（--fix 模式）：
    - 创建缺失的目录
    - 修复配置文件格式错误
    - 启用 auto-continue hook
- [ ] 实现 `ccautorun edit <task-name>` 命令（快捷编辑）
  - 支持 `--dry-run` 预览将要打开的文件
  - 在配置的编辑器中打开计划文档
  - **拆分架构**：打开 `README.md`（或用户指定的 stage 文件）
  - **单文件**：打开 `task-name.md`
  - 支持 `--stage N` 打开指定 stage 文件（仅拆分架构）
  - 从 config.yaml 读取默认编辑器（code/vim/nano等）
  - 支持 `--editor <name>` 临时指定编辑器
- [ ] 实现 `ccautorun validate <task-name>` 命令（计划验证）
  - 支持 `--dry-run` 预览验证操作
  - 验证计划文档格式：
    - 配置头是否完整（stages/current/architecture等）
    - Stage 文件命名是否规范（拆分架构）
    - Stage 数量是否匹配配置
    - 文件内容结构是否完整（目标/任务清单/完成标准等）
  - Token 效率检查：
    - 单个 Stage 是否超过 1500 tokens（拆分架构）
    - 单文件是否超过 5000 tokens
  - 给出具体的修复建议
  - 输出验证报告
- [ ] 实现 `ccautorun recover <task-name>` 命令（错误恢复）
  - 支持 `--dry-run` 预览恢复操作
  - 从错误状态恢复任务：
    - 检查并修复会话状态文件
    - 重新同步计划文档和会话状态
    - 修复 README.md 的 current 字段（拆分架构）
  - 提供交互式恢复向导：
    - 显示当前问题
    - 提供多个恢复选项
    - 用户确认后执行
  - 备份原始状态，支持回滚
- [ ] 实现 `src/utils/logger.js`（使用 winston）
  - 彩色输出（chalk + winston transports）
  - 日志级别（info, warn, error, debug）
  - 文件日志（写入.ccautorun/logs/）
  - **日志轮转**（关键！）：
    - 使用 winston-daily-rotate-file
    - 配置：最多保留 30 天或 100MB
    - 按日期归档（execution-2025-01-06.log）
  - 支持 --verbose 全局选项
  - 统一日志格式（时间戳 + 级别 + 消息）
- [ ] 实现 `src/utils/config.js`
  - 读取/写入配置文件
  - 默认配置管理
  - 支持配置合并（用户配置覆盖默认配置）
- [ ] 实现 `src/utils/config-validator.js`
  - 使用 ajv 验证 config.yaml 格式
  - 定义 JSON Schema
  - 提供清晰的验证错误提示
  - 支持配置版本检查
- [ ] 实现 `src/utils/error-handler.js`
  - 统一错误处理和格式化
  - 错误分类（用户错误 vs 系统错误）
  - 错误代码系统（E001, E002...）
  - 提供上下文相关的修复建议
  - 友好的错误消息模板

### ✅ 完成标准
- [ ] 首次运行显示交互式 Onboarding
- [ ] `ccautorun --help` 显示完整帮助
- [ ] `ccautorun init` 可以成功初始化项目
- [ ] **init 支持 --dry-run 预览**
- [ ] **init 支持 --setup-hooks 和 --enable-hooks 自动配置**
- [ ] init 可以自动创建和配置 `.claude/hooks.yaml`
- [ ] init 可以交互式询问是否启用 hook
- [ ] 初始化后目录结构正确（包含 archive/ 目录）
- [ ] config.yaml 包含 version 字段和新配置项
- [ ] config.yaml 验证可以捕获格式错误并给出清晰提示
- [ ] `ccautorun list` 可以列出计划（即使为空）
- [ ] `ccautorun status` 可以显示状态
- [ ] `ccautorun doctor` 可以检查所有依赖并输出诊断报告
- [ ] **doctor 可以检查 Claude CLI 版本并给出升级建议**
- [ ] **doctor 支持 --dry-run 和 --fix 自动修复**
- [ ] **edit 命令可以打开计划文档**（支持 --stage N）
- [ ] **validate 命令可以验证计划格式和 token 效率**
- [ ] **recover 命令可以从错误状态恢复任务**
- [ ] 错误处理统一且友好，包含错误代码和修复建议
- [ ] **日志支持轮转**（winston-daily-rotate-file）
- [ ] 日志正确输出到文件和控制台
- [ ] **所有命令支持 --dry-run 全局选项**
- [ ] 支持 --verbose 全局选项
- [ ] 无TypeScript/ESLint错误

### 📤 预期输出
- [ ] `src/cli.js` (300行，增加 Onboarding 和全局选项）
- [ ] `src/commands/init.js` (250行，增加 hook 自动配置）
- [ ] `src/commands/list.js` (150行)
- [ ] `src/commands/status.js` (100行)
- [ ] `src/commands/doctor.js` (300行，增加版本检查和 --fix）
- [ ] `src/commands/edit.js` (120行，新增）
- [ ] `src/commands/validate.js` (200行，新增）
- [ ] `src/commands/recover.js` (180行，新增）
- [ ] `src/utils/logger.js` (150行，增加 winston 和日志轮转）
- [ ] `src/utils/config.js` (80行，添加合并逻辑）
- [ ] `src/utils/config-validator.js` (120行，新增）
- [ ] `src/utils/error-handler.js` (150行，新增）

### ⚠️ 注意事项
- Commander.js v11使用ES Modules语法
- chalk v5也是纯ESM，注意import方式
- winston 需要配置 winston-daily-rotate-file
- 彩色输出考虑终端兼容性
- **错误处理至关重要**：统一使用 error-handler，包含错误代码和修复建议
- **配置验证**：使用 ajv，定义完整的 JSON Schema
- **doctor 命令是关键**：应该是用户遇到问题时的第一选择
- **--dry-run 全局支持**：所有有副作用的命令都应支持
- **hook 自动配置是用户体验关键**：降低首次使用门槛
- **CLI 输出语言**：所有输出只使用英语，不做多语言支持
- **Windows 兼容性**：避免使用 shell 特定命令（如 which），使用 Node.js API
- **Onboarding 应简洁明了**：3 个选项，快速上手
- **edit 命令跨平台**：检测可用编辑器（code/vim/nano等）

### 🧪 验证命令
```bash
# 测试首次运行 Onboarding
rm -rf .ccautorun
ccautorun
# 应显示欢迎界面和3个选项

# 测试 init（关键！）
cd /tmp/test-project
ccautorun init --dry-run          # 预览
ccautorun init --setup-hooks      # 自动配置 hook
ccautorun init --enable-hooks     # 自动启用 hook
# 应自动创建并配置 .claude/hooks.yaml

# 测试 doctor（关键！）
ccautorun doctor
# 应输出：Node.js 版本、Claude CLI 版本、Hook 配置状态等

ccautorun doctor --dry-run        # 预览修复
ccautorun doctor --fix            # 自动修复
# 应自动修复常见问题

# 验证配置文件
cat .ccautorun/config.yaml
# 应包含 version: "2.0.0" 和所有新配置项

# 测试配置验证
echo "invalid: [" >> .ccautorun/config.yaml
ccautorun list
# 应显示清晰的配置验证错误

# 测试 list
ccautorun list

# 测试 status
ccautorun status

# 测试新命令
ccautorun edit test-task                    # 打开 README.md
ccautorun edit test-task --stage 2          # 打开 stage 2
ccautorun validate test-task                # 验证计划格式
ccautorun recover test-task --dry-run       # 预览恢复
ccautorun recover test-task                 # 执行恢复

# 测试帮助
ccautorun --help
ccautorun init --help
ccautorun doctor --help
ccautorun edit --help
ccautorun validate --help

# 测试全局选项
ccautorun --verbose list
ccautorun --dry-run init

# 验证日志轮转
ls -lh .ccautorun/logs/
# 应看到按日期归档的日志文件
```

---

